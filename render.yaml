services:
  - type: web
    name: sgi-api-staging
    runtime: python
    buildCommand: "pip install -r requirements.txt"
    startCommand: "uvicorn main:app --host 0.0.0.0 --port 8000"
    plan: free
  - fromGroup: sgi-staging
    type: web